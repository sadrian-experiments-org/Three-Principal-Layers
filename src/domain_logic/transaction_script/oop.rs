pub struct Hotel;

impl Hotel {
    pub fn book_room(&self, room_number: u32) {
        // choose hotel
        // check availability
        // calculate price
        // book the room
        // commit the transaction
        println!("It works!")
    }
}